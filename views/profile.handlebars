<div class="row disc-listing">
    <div class="col-auto disc-listing">
        {{!-- this will require Including the User model and name attribute in the Home Route for /listing in order to
        properly show the Username --}}
        <h2 class="font-black">Welcome {{user.name}},</h2>
        <div class="log-page-profile">
            {{> listing-details}}
            <table class="table_block-profile mpitems push_down table_responsive">
                <thead>
                    <tr>
                        <th class="info_head" colspan="2">Disc List</th>
                        {{!-- <th class="seller_head">Users</th> --}}
                        <th class="price_head">$</th>
                        <th class="format_head">Vinyl, CD, Cassettes</th>
                        {{!-- <th class="add_save">Save</th> --}}
                        <th class="add_save">Remove</th>
                        {{!-- <th class="add_cart">Purchase</th> --}}
                    </tr>
                </thead>
                <tbody>
                    <tr class="disc_item">
                        {{#each listingData as |listing|}}
                        <td class="disc_img">
                            {{#if listing.cover_art}}
                            <img class="cover-art" src="{{listing.cover_art}}" alt="{{listing.artist}}" />
                            {{else}}
                            <img class="cover-art" src="/img/disc-icon.png">
                            {{/if}}
                            {{!-- <a href="/listing/{{disc.id}}">{{disc.artist}}</a> --}}
                        </td>
                        <td class="disc_desc">
                            <a href="/listing/{{listing.id}}"><strong>{{listing.album_title}}</strong></a>
                            <br>
                            <p>Artist: {{listing.artist}}</p>
                            <br>
                            <p>Condition: {{listing.condition}}</p>
                            <br>
                            <p>Description: {{listing.description}}</p>
                        </td>
                        {{!-- it's already the logged in user's profile, only their own listings will show up here so displaying user.name isn't necessary under the seller thing --}}
                        {{!-- <td class="seller_info">Seller: {{user.name}}</td> --}}
                        <td class="item_price">Price: ${{listing.price}}</td>
                        <td class="item_format">Format: {{listing.format}}</td>
                        <td class="remove_button">
                            <button id="rmListBtn" class="btn btn-sm btn-danger listingDeleteBtn"
                                data-id="{{listing.id}}">Remove Listing</button>
                        </td>
                        {{!-- this is the seller's own listings, they would not need the ability to add their own listing to their cart --}}
                        {{!-- <td class="cart_button">
                            <button id="plusCartBtn" class="btn btn-sm btn-danger addCartBtn"
                                data-id="{{listing.id}}">Add to Cart</button>
                        </td> --}}
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{!-- <div class="profile-div">
                <h2>Your Listings</h2>
                {{#each listingData as | listing |}}
                <section class="listing">
                    <img class="img-profile" src="{{listing.cover_art}}"
                        alt="Cover Art for {{listing.album_title}} by {{listing.artist}}">
                    <div class="listing-details">
                        <h3 class="title-profile-h3">{{listing.album_title}}</h3>
                        <p>By: {{listing.artist}}</p>
                        <p>Genre: {{listing.genre}}</p>
                        <p>Format: {{listing.format}}</p>
                        <p>Condition: {{listing.condition}}</p>
                        <p>Description: {{listing.description}}</p>
                        <p>Price: {{listing.price}}</p>
                    </div> --}}
                    {{!-- if we get the time this would be a great place to include an Update Listing --}}
                    {{!-- <div class="col-md-4">
                        <button class="btn btn-sm btn-danger listingDeleteBtn" data-id="{{listing.id}}"> Delete
                            Listing</button>
                    </div> --}}
                    {{!--
                </section>
                {{/each}}
            </div> --}}
        </div>
    </div>
</div>
{{!-- <div class="row mt-4 disc-listing">
    <div class="col-md-6 ">
        <h3 class="font-black">Create New Listing</h3> --}}
        {{!-- present the full listing form! --}}
        {{!--
    </div> --}}

    {{!-- #ifEq does not work, would need separate npm package I can't find
    #with does not work how I thought it did, need to adjust all other pathways --}}

    <script src="/js/profile.js"></script>
    <script src="/js/deletelisting.js"></script>