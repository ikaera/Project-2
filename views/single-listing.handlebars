{{!-- nothing here seems to be working, not sure at all as to why! --}}
{{!-- however, at least, it is trying to load this page, as it is displaying the broken version of the cover art if/else statement --}}
{{!-- <div class="row">
    <div class="col-auto">
        <h2>Hey there, {{User.name}}.</h2>
    </div>
</div> --}}
<table class="table_block-profile mpitems push_down table_responsive">
                <thead>
                    <tr>
                        <th class="info_head" colspan="2">Disc List</th>
                        <th class="seller_head">Users</th>
                        <th class="price_head">$</th>
                        <th class="format_head">Vinyl, CD, Cassettes</th>
                        {{!-- <th class="add_save">Save</th> --}}
                        <th class="add_save">Save</th>
                        <th class="add_cart">Purchase</th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="disc_item">
                        
                        <td class="disc_img">
                            {{#if listing.cover_art}}
                            <img class="cover-art" src="{{listing.cover_art}}" alt="{{listing.artist}}" />
                            {{else}}
                            <img class="cover-art" src="/img/disc-icon.png">
                            {{/if}}
                            {{!-- <a href="/listing/{{disc.id}}">{{disc.artist}}</a> --}}
                        </td>
                        <td class="disc_desc">
                            <a href="/listing/{{listing.id}}"><strong>{{listing.album_title}}</strong></a>
                            <br>
                            <p>Artist: {{listing.artist}}</p>
                            <br>
                            <p>Condition: {{listing.condition}}</p>
                            <br>
                            <p>Description: {{listing.description}}</p>
                        </td>
                        <td class="seller_info">Seller: {{listing.user.name}}</td>
                        <td class="item_price">Price: ${{listing.price}}</td>
                        <td class="item_format">Format: {{listing.format}}</td>
                        <td class="addFavoriteBtn">
                            <button id="addFavoriteBtn" class="btn btn-sm btn-danger addFavoriteBtn"
                                data-id="{{listing.id}}">Save To Favorites</button>
                        </td>
                        <td class="cart_button">
                            <button id="addCartBtn" class="btn btn-sm btn-danger addCartBtn"
                                data-id="{{listing.id}}">Add to Cart</button>
                        </td>
                    </tr>
                    
                </tbody>
            </table>
    <script src="/js/saveheart.js"></script>
    <script src="/js/addtocart.js"></script>


{{!-- <div class="row mb-4 listings">
    <div class="col-md-5">
        {{#if listing.cover_art}}
        <img class="cover-art" src="{{listing.cover_art}}" alt="Cover Art for {{listing.album_title}} by {{listing.artist}}" />
        {{else}}
        <img class="cover-art" src="/img/listing-icon.png">
        {{/if}}
        <a href="/listing/{{listing.id}}">{{listing.artist}}</a>
        <h2>{{listing.album_title}}</h2>
        <p>By: {{listing.artist}}</p>
        <p>Genre: {{listing.genre}}</p>
        <p>Format: {{listing.format}}</p>
        <p>Condition: {{listing.condition}}</p>
        <p>Description: {{listing.description}}</p>
        <p>Price: {{listing.price}}</p>
        <p>Seller: {{listing.user.name}}</p>
    </div>
    <div>
      <button id="addFavoriteBtn" class="btn btn-sm btn-danger addFavoriteBtn" data-id="{{disc.id}}">Save To Favorites</button>
    </div>
    <div>
      <button id="addCartBtn" class="btn btn-sm btn-danger addCartBtn" data-id="{{disc.id}}">Add To My Cart</button>
    </div>
</div>  --}}